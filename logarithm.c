#include <stdio.h>
#include <math.h>

int main () 
{
   double x,y, result;
   x = 5;
   y=7;

   /* finding log(2.7) */
   
   result = log(x)+log(y);
   printf("Product = %d", result);
   
   return(0);
}
